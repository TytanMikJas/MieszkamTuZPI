## Prerender

### Testing

Emulate a normal user request:
TODO add correct url

```bash
curl -A Chrome localhost/mapa/start
curl -A Chrome https://url.mieszkamtu.pl/mapa/start
```

Emulate a crawler request:
TODO add correct url

```bash
curl -A Googlebot localhost/mapa/start
curl -A Googlebot https://url.mieszkamtu.pl/mapa/start

```
